{
  "name": "sloptrim",
  "version": "0.9.1",
  "description": "Scores the first 256 KB of extracted prose from files accepted up to 512 KB for plain text and 4 MB for supported archives, against a catalogue of 71 documented patterns, 62 of them with a detector behind them and 50 able to move the score. It names the ones that fired so the flagged spans get fixed before the file ships. The 0 to 100 score lands in one of five bands: clean, light tells, mixed, heavy tells, pervasive tells. Every band describes the prose and none of them names an author. Reads 20 formats including .docx and .epub. Deterministic, Python standard library only, no network, no model. Prose only; code, config and commits stay untouched.",
  "author": {
    "name": "Seyed Ehsan Hadi",
    "url": "https://github.com/seyedehsanhadi"
  },
  "homepage": "https://github.com/seyedehsanhadi/sloptrim",
  "license": "Apache-2.0",
  "keywords": [
    "humanizer",
    "ai-slop",
    "de-slop",
    "prose",
    "prose-linter",
    "writing",
    "writing-quality",
    "text-analysis",
    "stylometry",
    "markdown",
    "docx",
    "linter",
    "zero-dependency",
    "claude-code"
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/sloptrim-activate.js\""
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/sloptrim-subagent.js\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/sloptrim-tracker.js\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/sloptrim-guard.js\""
          }
        ]
      }
    ]
  }
}
